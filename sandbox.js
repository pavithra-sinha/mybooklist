const names = ['Tom', 'Jerry', 'Spike', 'Pokemon', 'Doremon']
const password = 'Pass';

if (password.length > 8) {
    console.log('Password is long enough!!!')
} else {
    console.log('password is too small')
}